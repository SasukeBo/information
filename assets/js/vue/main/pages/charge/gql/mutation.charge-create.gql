#import '../../device/charge/gql/fragment.device-charge.gql'

mutation (
  $deviceUUID: String!
  $userUUID: String!
  $privIDs: [Int]!
) {
  deviceCharge: deviceChargeCreate(
    uuid: $deviceUUID
    userUUID: $userUUID
    privIDs: $privIDs
  ) {
    ...deviceCharge
  }
}
